# YO-DEE POS — สถานะโปรเจกต์ (Project Status)

**อัปเดตล่าสุด:** 2026-06-12 · prod commit `fbd638f`

ไฟล์นี้คือ snapshot สถานะปัจจุบันของระบบ Mobile Queue → POS ของร้าน YO-DEE Yogurt
(ดูประวัติฟีเจอร์เต็มได้จาก git log; ฉบับนี้สรุปสิ่งที่ "อยู่บนระบบจริง" ตอนนี้)

---

## 🌐 Environments

| | URL | DB | สถานะ |
|---|---|---|---|
| **Production** (ร้านจริง) | https://mobile-queue.onrender.com | Turso (durable, primary = Tokyo `aws-ap-northeast-1`) | 🟢 LIVE — ใช้งานจริง |
| **UAT** (ทดสอบ) | https://mobile-queue-uat.onrender.com | node:sqlite (ephemeral, รีเซ็ตทุก boot) | 🟢 สำหรับทดลองของใหม่ |

- โฮสต์: **Render free, region Singapore** · GitHub: `chatrk50/mobile-queue` (public)
- หน้าใช้งาน: `/cashier/` (แคชเชียร์) · `/liff/` (ลูกค้า LINE) · `/display/` (จอคิว) · `/status/` (หน้าสถานะมือถือ)
- **Keep-alive:** GitHub Action ปลุกทั้ง 2 เซิร์ฟทุก ~3 นาที (06:00–24:00 BKK) ไม่ให้หลับ — เช็คล่าสุด: ทั้งคู่ตอบ ~0.2 วิ (ตื่น)

---

## 🔑 สิ่งที่ต้องรู้

- **ระบบสะสมแต้ม (Loyalty) = ปิดอยู่บน prod** (`/api/loyalty/config` → `enabled:false`)
  โครงสร้างพร้อมหมด เปิดเมื่อพร้อม: แคชเชียร์ → ⚙ → "แต้มสะสม/ของรางวัล" → ติ๊ก "เปิดใช้งานสะสมแก้ว" · UAT เปิดให้ทดลองอยู่
- **ความเร็ว:** prod save ข้อมูลที่ Turso โตเกียว (~65ms/write) ขณะแอปอยู่สิงคโปร์ → มี floor ~0.5 วิ/action
  Turso **ไม่มี region สิงคโปร์** ให้เลือก (มีแค่ Tokyo/Mumbai/Ireland/US) ย้ายใกล้กว่านี้ไม่ได้
  Render paid **ไม่ช่วย** เรื่องนี้ (พิสูจน์แล้ว: UAT free tier เดียวกันยังเร็ว = ไม่ใช่ CPU) — paid แก้แค่ cold-start

---

## ✅ ฟีเจอร์หลักที่ LIVE บน prod

**ลูกค้า (LINE/LIFF):** สแกน QR → สั่ง → จ่ายก่อนรับคิว (พร้อมเพย์ไดนามิก/KBank กรอกยอด/จ่ายที่เคาน์เตอร์) · รวมบรรทัดเมนูซ้ำ · สั่งเหมือนเดิม · ดูสลิป/สถานะคิวสด

**แคชเชียร์:** จอสั่งแยกซ้าย-ขวา · **กดชำระ → เลือกวิธีจ่ายเด้งทันที → สร้าง+จ่าย+ออกคิว ครั้งเดียว** · **ปุ่ม "พักบิล (จ่ายทีหลัง)"** · รับเงินบิลค้าง · ยกเลิก/คืนเงินมีเหตุผลสำเร็จรูป · ออเดอร์ค้างหายเองเมื่อเกินเวลา · ป้ายของใกล้หมด

**จัดการ (เจ้าของ):** หน้าเลือกซ้าย-แก้ขวา (เมนู·สาขา·สต๊อก·พนักงาน) · ราคา Delivery แยก · เวลาเปิด-ปิดรายสาขา · สูตร BOM ตัดสต๊อก · **กราฟภาพรวม** (ยอด 7 วัน·เมนูขายดี·ช่วงพีค·ช่องทาง) · รายงานละเอียด + Excel · **🧹 ล้างข้อมูลทดสอบ** (เริ่มต้นใหม่ เก็บ config) · คีย์แพด PIN

**ระบบสะสมแต้ม (พร้อม แต่ปิดบน prod):** สะสม 1 ดวง/แก้ว (LINE-only) · โบนัสออเดอร์แรก · 🎂 วันเกิด · 👫 ชวนเพื่อน · 🏅 badge · แลกที่เคาน์เตอร์ (ยอด 0 ปิดบิลเอง) · 🎉 wow ออเดอร์แรก · nudge ตอนสั่ง · ยกเลิกก่อนเสิร์ฟคืนแต้ม/หลังเสิร์ฟไม่คืน · รูปของรางวัล

---

## ⚡ Perf measures ที่ทำไปแล้ว (prod)

1. Cache prepared statements (db.js) — ไม่ compile SQL ซ้ำทุก query
2. เลื่อน SSE broadcast ออกจาก response path (events.js, setImmediate)
3. เลื่อน audit log (sale_events) + บันทึกลูกค้า ออกจาก critical path
4. **รวม create+pay ของแคชเชียร์เป็น request เดียว + tender picker เด้งทันที** (ล่าสุด)
5. Duplicate-order fix: atomic `INSERT … WHERE NOT EXISTS(active ticket)`

6. **Optimistic UI ตอนกดจ่าย/พักบิล** (🟡 อยู่บน **UAT** แล้ว — รอเจ้าของลองฟีล ก่อนขึ้น prod): กดจ่าย→เคลียร์บิล+เสียง "ติ๊ง"+กลับหน้าคิว **ทันที (0ms)** แล้วยิง network เบื้องหลัง · เลขคิวเด้งอัปเดต toast เองตอน server ตอบ · network หลุด = **กู้บิลคืนอัตโนมัติ** (ไม่เสียออเดอร์) · server ยังเป็นเจ้าของเลขคิว (ไม่มีคิวชน)

**ยังไม่ทำ (ถ้าอยากเร็วกว่านี้):**
- **Outbox + auto-retry เต็มรูป** — ตอนนี้ network fail = กู้บิลให้กดใหม่ (manual) · ขั้นถัดไป = retry อัตโนมัติ
- **Batch DB writes** (รวบ ~12 round-trips → ~3-4) — ต้องทดสอบกับ libsql จริงก่อน (UAT เป็น node:sqlite ทดสอบ path นี้ตรงๆ ไม่ได้)

---

## 📋 Owner to-do

- [ ] **ล้างข้อมูลทดสอบบน prod** (⚙ → ตั้งค่าระบบ → 🧹) ก่อนเริ่มนับยอดจริง — *แล้วทุกออเดอร์ต่อจากนั้นเป็นยอดจริง*
- [ ] **เปิดร้าน:** กดเปิดโซนที่จอแคชเชียร์ (ตอนนี้ค่าเริ่ม = ปิด)
- [ ] **Render paid ($7/เดือน)** เมื่อใช้จริง 100% — กัน cold-start หลับ (ไม่เกี่ยวกับความเร็ว save)
- [ ] **เปิด Loyalty บน prod** เมื่อพร้อมโปรโมต (+ ป้าย QR + เทรนพนักงานเชียร์ไลน์)
- [ ] (ทางเลือก) ตั้งสูตร BOM ของเมนู เพื่อตัดสต๊อก + เช็คเมนูหมดอัตโนมัติ

---

## 🔌 รอของ/คีย์ (ฟีเจอร์ dormant)

- เครื่องปริ้นใบเสร็จ/สลิปครัว (โค้ดพร้อม รอเสียบเครื่อง)
- ตรวจสลิปอัตโนมัติ SlipOK (รอพร้อมเพย์ร้านค้าจริง — K SHOP เดิมใช้ไม่ได้)
- LINE แจ้งเตือนเจ้าของ (รอ LINE token)
